import { FormatEffortsPipe } from './format-efforts.pipe';

describe('FormatEffortsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEffortsPipe();
    expect(pipe).toBeTruthy();
  });
});

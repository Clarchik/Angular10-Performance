import { DraaggableDropZoneDirective } from './draaggable-drop-zone.directive';

describe('DraaggableDropZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DraaggableDropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
